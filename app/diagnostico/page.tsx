import { DiagnosticWizard } from "@/components/diagnostic/diagnostic-wizard";

export const metadata = {
  title: "Diagnóstico — AI Fluency LATAM",
};

export default function DiagnosticPage() {
  return <DiagnosticWizard />;
}
