import { RoadmapBoard } from "@/components/roadmap/RoadmapBoard";

export const metadata = {
  title: "Roadmap — AI Fluency LATAM",
};

export default function RoadmapPage() {
  return <RoadmapBoard />;
}
